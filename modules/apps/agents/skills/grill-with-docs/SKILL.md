---
name: grill-with-docs
description: Relentlessly interview a plan or design while retaining durable domain context and decisions in Hindsight.
disable-model-invocation: true
---

Call the Skill tool twice, for `grilling` and `domain-modeling`.

Persist durable terms, constraints and decisions to Hindsight bank `rodrigo` with project/source tags as they settle. Do not create or update `CONTEXT.md`, ADR files or repo-local agent-memory documents. Keep deterministic public engineering contracts in the repository only when contributors or CI need them without Hindsight access.
